{
  "name": "r2-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for multiple R2 buckets",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
