{
  "name": "r2-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for multiple R2 buckets",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "backfill-cache-control": "node scripts/backfill-cache-control.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.840.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
